# GOAL — 交付 ADDA Growth OS V1.0

## 一句话目标

构建可自托管、可验收、移动端可用的 ADDA TEA 增长运营 Web 产品，使品牌负责人和门店人员能把品牌知识转成经审批的三语言内容与校园活动，以第一方链接、会员和 POS 订单形成可核验的获客、首购、复购与推荐闭环。

不是做七个聊天框，不是生成静态大屏，不是承诺 AI 自主经营，不是保证营收或粉丝增长。

## 项目假设与事实边界

首家店位于甲方描述的达卡大学周边；部署默认 timezone=Asia/Dhaka、currency=BDT；后台 zh-CN/en，消费者端 en/bn。实际地址、开业日、价格、菜单、会员权益、品牌色、Logo、SHAPLI 原图、平台凭证都尚未提供。
用户文本中已有的品牌名、口号、栏目名是“甲方输入”，需入库并经负责人确认，不等于其他细节已确认。未知字段留空并显示 needs_input，不能编造 18 款产品、真人 KOC、合作学校或历史营收。
开发可以使用独立 DEMO tenant 的合成数据；不得因此阻塞基础开发，也不得以合成数据冒充联调完成。

## 四条必须跑通的纵向闭环

A. 内容获客：批准品牌事实 → 创建活动 → 生成内容包 → 本地语言复核 → 审批 → 导出/授权发布 → 独立来源链接 → 注册会员/领取券 → 收银录入订单引用 → POS 导入核验 → 活动归因与报告。
B. 校园活动：登记有来源的社团/KOC → 合作审批 → 专属链接/码 → 活动报名与现场核验签到 → 有效首购 → 推荐奖励草稿 → 退款冲销与复盘。
C. 会员复购：注册与独立营销同意 → 已识别订单 → 规则分群 → 模板与人群审批 → 发送前再检查同意/频控 → 授权触达或显式人工流程 → 有效复购统计；保留未触达对照组能力。
D. 顾客声音：授权获取或人工导入评价 → 分类与原文证据 → 高风险升级 → 员工处理任务 → 回复草稿审批 → 发送/人工回填 → 关闭与次日复盘。

## V1 范围

必须交付：品牌事实与素材库、真实产品/价格维护、三语言内容工作室、内容日历、活动/KOC 台账、第一方落地页与来源码、ADDA MATE 轻 CRM、券核验与推荐记录、POS CSV 导入与对账、六项指标的可解释视图、评论/客服工单、每日一页报告、审批任务中心、连接器状态、审计与部署手册。
保留总部/门店数据边界；第一期不交付加盟结算、总部跨店智能排名或完整 ERP。

默认首个 live connector 为 WhatsApp Business Platform，仅在甲方账户、同意、模板和平台权限具备后启用。其他平台先用批准的导出包、人工发布链接和指标导入闭环。不得伪装为已自动接通。

## 固定实现方向

采用 TypeScript 模块化单体：Next.js Web/API + 独立 Node worker + PostgreSQL + Prisma + pg-boss + Zod + Better Auth + Vercel AI SDK 的 provider adapter。媒体使用可替换对象存储接口，本地开发磁盘、生产 S3-compatible storage。
七个专业能力是有类型输入输出的 skill handlers，不是七个独立服务。业务状态、审批和资金约束由数据库与代码控制；模型只能查询批准的指标和提出草稿。
基础检索使用已批准结构化事实及全文检索；多语言跨文档检索需要时再加入经过测试的多语言 embedding/pgvector，不能把向量库当事实来源。

不同时引入 Dify、n8n、LangGraph、Mautic、完整 Chatwoot 和完整 CRM。第三方项目主要参考其模块设计；可选系统作为独立适配器集成。

## 完成定义

1. 从干净仓库和新数据库能够按 README 启动、迁移、初始化，登录后各页面均使用后端真实持久化数据。
2. 四条闭环有 Playwright 或集成测试证明；手工发布、外部平台不可用和真实发布必须分别展示。
3. 所有营收、归因、首购、复购数字来自代码计算；通过 fixtures 的精确预期；缺失为 null + reason，不伪造零或改善幅度。
4. 品牌事实、价格、日期、引用、内容版本、孟语复核状态和审批记录可追溯。没有本地复核的 bn 文案不能发布。
5. 退订、越权、重复核销、重复回调、退款、跨店访问、改稿后旧审批、模型幻觉和外部接口失败有明确测试。
6. 生产无 demo 密码、无模拟送达、无未脱敏日志；可备份、恢复、暂停外发与导出甲方数据。
7. 每个目标附测试命令、结果、截图/日志路径与未完成项。不能以 test.skip、空测试或 mock-only 结果宣布真渠道接通。

## 分级验收

DEMO_READY：合成数据和模拟渠道跑通，仅开发演示。
CORE_ACCEPTED：甲方数据可导入、核心业务与人工闭环可运行；AI 能力经真实模型调用和本地语言人员抽验。
LIVE_CHANNEL_ACCEPTED：合同指定渠道完成真实授权、批准内容的受控发送和状态回执；证据脱敏保存。
PRODUCTION_READY：CORE_ACCEPTED、适用 live channel 验收、权限安全、备份恢复、客户配置和 UAT 签字均完成。
无外部凭证只可将相应项标 external_blocked，不能标 done；允许继续无依赖目标。

## 非目标

不承诺替代 3–5 个员工、固定到店增幅或会员增长；不抓取私人学生资料、不推断性别/宗教/关系网络、不自动陌生私信；不自动开广告预算、发现金奖励、支付 KOC 或伪造评论；不默认跨平台曝光去重；不把扫码当到店；不把归因销售当增量销售。
不在 V1 重建 POS、库存预测、供应链、排班、自动视频制作或原生手机 App。

## 执行顺序

依 TASKS.json：G00 基础 → G01 品牌 → G02 订单指标 → G03 获客到成交闭环 → G04 内容 → G05 校园活动 → G06 CRM → G07 顾客声音 → G08 指挥台 → G09 真渠道 → G10 上线验收。
先完成一个纵向切片，再扩展其他功能。每轮从最早未完成且依赖满足的目标开始。
