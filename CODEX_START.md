# 交给 Codex 的启动指令

## 第一轮：复制以下文字

你是本项目的交付工程师。请先读取根目录 AGENTS.md、GOAL.md、TASKS.json、.agent/PLANS.md 和 goals/G00.md，再检查仓库现状。该仓库当前可能只有执行规格，不是已有完整应用。
按目标实现可运行产品，不要只输出方案或生成静态页面。先创建/更新 G00 ExecPlan，再完成 G00 的代码、迁移、基础权限和实际验证。已有代码保留并增量改造。
缺少菜单、Logo、开业日、平台凭证时，使用独立 DEMO tenant 的显式测试资料，并建立 needs_input/external_blocked 状态；不要编造甲方资料，也不要因此停止无依赖开发。
严格执行 AGENTS.md，记录实际运行的测试、结果和证据路径，更新 TASKS.json。完成 G00 后给出可操作的验证步骤及 G01 入口。不要把整个 V1 一次性宣布完成。

## 每个后续目标

读取 AGENTS.md、GOAL.md、TASKS.json、当前 ExecPlan 和上次检查点。找出依赖已满足的最早未完成目标，按该目标的验收条件实施并验证。不要重写已通过的模块，不要绕过测试，不要把外部阻塞伪装成成功。完成后更新证据、任务状态和下一目标。

## 独立审查轮

请独立审查最近完成目标。不要依据实现者的总结判断完成。阅读验收文件和实际代码，从一个干净测试数据库启动，运行测试并尝试越权、重试、退款、缺字段、错误审批与退订边界。列出可复现缺陷和证据；验证前不要修改验收标准。没有真实平台凭证时明确标记真渠道未验收。

## 甲方演示轮

按 spec/ACCEPTANCE.md 的 UAT 场景演示：创建活动 → 生成并批准内容 → 导出发布 → 来源码 → 会员同意 → 收银订单引用 → POS 导入 → 归因报告 → 分群触达 → 复购报告。清楚展示哪些步骤是人工、哪些是模拟、哪些是真 API。演示不能用浏览器内存或写死数字代替持久化链路。
