# G03 — 活动到成交的第一个完整切片

依赖：G01, G02。当前状态：未开始。

## 可交付结果

实现活动、第一方来源链接、H5 注册、同意、券 reservation、收银 POS 引用、核验成交与唯一主归因。这个目标完成后已有可演示的业务闭环。

## 必读

AGENTS.md、GOAL.md、spec/PRODUCT.md、spec/ARCHITECTURE.md、spec/DATA_MODEL.md、spec/METRICS.md、spec/ACCEPTANCE.md；涉及 AI/渠道时再读对应规格。

## 实施切片

1. 建立活动与条款版本、来源 token、落地页和 independent opt-in。
2. 实现会员联系方式验证 adapter；离线仅允许 test outbox，生产无验证渠道时不标 verified。
3. 实现券发放、员工 reservation、POS 匹配、并发防重与失败对账。
4. 实现 primary_source_v1，端到端验证来源→会员→订单→报告且总额对平。

## 必须通过的验收

A24, A25, A27, A28, A29, A30, A31, A32，具体条件见 spec/ACCEPTANCE.md。把相应用例写入可运行测试，不能只写验证描述。

## 证据

手机 H5 到收银到订单报告的 E2E 录像/截图、20 并发核销测试、主归因对账结果。
每个证据放 artifacts/G03/，记录 exact command、exit code、运行日期、依赖条件与已知限制。真实业务测试不可只调用 fixture verifier。

## 完成和续跑

实际通过后更新 TASKS.json；失败保留 in_progress。依赖外部凭证/真人审核的部分标 external_blocked，不造假；继续其他没有依赖的切片。记录下一目标、回滚方式与未完成事项。
