# G00 — 基础骨架、权限与可运行环境

依赖：无。当前状态：未开始。

## 可交付结果

建立 TypeScript 工作区、真实数据库和迁移、认证、tenant/store 权限、worker、国际化骨架、空库引导、健康检查与 CI。只做少量真实页面，不能填充静态假数据。

## 必读

AGENTS.md、GOAL.md、spec/PRODUCT.md、spec/ARCHITECTURE.md、spec/DATA_MODEL.md、spec/METRICS.md、spec/ACCEPTANCE.md；涉及 AI/渠道时再读对应规格。

## 实施切片

1. 检查现有仓库并记录版本选择，创建 ExecPlan。
2. 实现 apps/web、apps/worker、packages/domain/db/adapters/ai/testing 的最小结构。
3. 实现真实认证与六类角色的权限骨架、DEMO 与空生产初始化分离。
4. 实现持久化任务和测试命令，启动并完成首次迁移、登录与权限测试。

## 必须通过的验收

A01, A02, A03, A04, A05, A57，具体条件见 spec/ACCEPTANCE.md。把相应用例写入可运行测试，不能只写验证描述。

## 证据

干净环境启动日志、登录与空库截图、权限测试输出、锁文件和版本矩阵。
每个证据放 artifacts/G00/，记录 exact command、exit code、运行日期、依赖条件与已知限制。真实业务测试不可只调用 fixture verifier。

## 完成和续跑

实际通过后更新 TASKS.json；失败保留 in_progress。依赖外部凭证/真人审核的部分标 external_blocked，不造假；继续其他没有依赖的切片。记录下一目标、回滚方式与未完成事项。
