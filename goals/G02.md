# G02 — POS 导入、身份映射与指标引擎

依赖：G00。当前状态：未开始。

## 可交付结果

先建立事实和计算，不让 AI 决定营收与复购。实现订单/退款 CSV staging、映射预览、幂等提交、对账、数据截止和固定指标服务。

## 必读

AGENTS.md、GOAL.md、spec/PRODUCT.md、spec/ARCHITECTURE.md、spec/DATA_MODEL.md、spec/METRICS.md、spec/ACCEPTANCE.md；涉及 AI/渠道时再读对应规格。

## 实施切片

1. 按 DATA_MODEL 和 METRICS 实现订单、退款、源水位和身份引用 schema。
2. 实现可重试的文件导入、预览确认、错误行隔离、重复文件与订单防重。
3. 将 golden fixture 导入真实测试数据库，按代码计算所有 expected_metrics。
4. 暴露授权 MetricQuery API，显示 coverage、unknown、source refs 与 formula_version。

## 必须通过的验收

A15, A16, A17, A18, A19, A20, A21, A22, A23, A26, A64，具体条件见 spec/ACCEPTANCE.md。把相应用例写入可运行测试，不能只写验证描述。

## 证据

标准导入模板、预览/对账截图、golden 应用测试输出、退款与时区回归结果。
每个证据放 artifacts/G02/，记录 exact command、exit code、运行日期、依赖条件与已知限制。真实业务测试不可只调用 fixture verifier。

## 完成和续跑

实际通过后更新 TASKS.json；失败保留 in_progress。依赖外部凭证/真人审核的部分标 external_blocked，不造假；继续其他没有依赖的切片。记录下一目标、回滚方式与未完成事项。
