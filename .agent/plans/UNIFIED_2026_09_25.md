# Unified surfaces and durable orchestration — uploaded-source implementation

## 目标和用户可见结果
以用户上传 adda-growth-os-source-2026-09-25.zip 为唯一基线，交付完整源码，统一 SUIWU 母品牌 / ADDA 子品牌；运营、审核、收银及顾客页面复用 UI；修复控制台范围与工具授权，提供持久化后台编排、取消和受控重试。不用独立 HTML 演示代替应用。

## 仓库现状与依赖
现有 Node HTTP + TypeScript + file-dev JSON 仓储，两个 HTML renderer，七个确定性只读 skill。遵循现有 D007/D039 增量改造，不在本轮悄悄将整仓改为新框架。PostgreSQL、受控生产初始化、真实渠道/模型验收仍需单独实现/验证。既有 116 项 TypeScript 测试基线全部通过。

## Progress
- [x] 解包并读取 AGENTS/GOAL/TASKS/架构、数据、AI 与验收合同；原始构建、116 项 TS 测试。
- [x] 集成旧补丁中经审查的源代码，重新执行整仓测试而非隔离替身。
- [x] 编排后台执行、实时授权、取消/超时/幂等、输出验证及检查点。
- [x] 统一所有现有业务页，增加审核/收银入口，共用主题、品牌、接口和权限。
- [x] 新增真实 HTTP、文件仓储并发恢复及 DOM + 真实 HTTP 桥交互回归。
- [ ] 浏览器原生导航、Cookie 与存储验收（环境策略阻断，不修改策略）。
- [x] 更新任务/能力/阻塞、验证脚本；最终压缩包生成时核对文件与哈希。

## 设计与文件路径
packages/domain/src/control-*；packages/adapters/src/control-worker.ts；apps/worker/src/index.ts；apps/web/src/ui/；packages/ui/src/；tests/unified-*.test.ts；artifacts/unified-2026-09-25/。

## 实施步骤
先领域与 HTTP，后 worker，再共享 UI 与具体入口，最后真实应用浏览器验收、文档与打包。每一阶段保留原有测试。

## 验证命令及预期
npm run build/typecheck；node --test dist/tests/*.test.js；node --test tests/validation-evidence.test.mjs；npm run test:ai；python tests/browser_unified.py。整仓使用真实指标与 file-dev 仓储，外部调用不发出。

## 实际验证证据
artifacts/unified-2026-09-25/baseline-build.log 和 baseline-tests.tap。npm ci 网络 DNS EAI_AGAIN，完整记录 npm-ci-network-error.log。使用环境预装 TypeScript 5.8.3 / Node 22.16.0 / 已安装 @types/node 完成完整编译；未伪装锁版本 clean install。后续继续记录具体工具链。

## Surprises & Discoveries
仓库规范开头声称仅规格，但实际已有运行应用；依现有源文件工作。不以旧截图证明本轮代码。现有 worker 仅处理 maintenance，新增编排业务执行必须在此基础上实现。

## Decision Log
保留原技术路线的本地适配实现；不把 UI 中的成功当业务增长或外部送达。品牌原图保持原字样，无授权扩展用途。

## 回滚与幂等
回到上传源码即可回滚代码；新增 run 字段向后兼容。测试和 seed 在独立临时目录，不改客户数据库；不做生产部署或外部支出。

## Outcomes & Next Checkpoint
当前 checkpoint：完整 source + 预编译 dist 本地交付。实际验证清单见 artifacts/unified-2026-09-25/validation.json 与 browser-checks.json，不使用旧截图替代。下一目标是独立环境执行 npm ci 和原生浏览器脚本，然后推进受控生产初始化/生产数据库/真实模型及外发 worker；这几项不因本次本地测试通过而标完成。


## 本轮后续证据
全应用测试当前为 145 项（139 TS 与 6 MJS），DOM/真实 HTTP 桥检查 89 项。此计数需对照最终实际命令日志。测试使用真实应用 HTTP 与文件仓储，不再是前几轮的指标替身。真实子进程退出用例验证提交前中断、死锁文件恢复和非重复上游执行。原生导航尝试的 ERR_BLOCKED_BY_ADMINISTRATOR 与 npm ci 的 EAI_AGAIN 都单列，不当成功。
