# 第三轮审查修复

## 目标和用户可见结果
统一订单身份、归因时间窗口和有效期校验，使 B01–B05 的错误结果被纠正或在写入前拒绝。新增源码也必须使旧验收记录失效（A01）。保留 N01–N08 修复与离线能力边界。

## 仓库现状与依赖
当前基线是 ADDA_Growth_OS_N01_N08_fixed_source.zip 对应源码。上一轮本地门禁为 85 项测试通过。用户本轮只提供审查 Markdown，未提供原始复现脚本；本轮增加等价回归，不声称运行用户原脚本。使用现有 Node/TypeScript 工具链，不增加依赖。

## Progress
- [x] 增加五个反例与两个正向对照，记录修改前结果。
- [x] 统一订单解析和归因窗口；歧义引用拒绝或保留 unknown。
- [x] 保存及执行前统一日期校验，拦截历史错误数据。
- [x] 验收记录比较完整源码集合、内容及工具链。
- [x] 运行全量门禁、更新能力台账并交付源码包。

## 设计与文件路径
在 packages/domain/src 中维护订单身份、归因模型和日期规则；路由只解析输入，仓储在写锁内验证并绑定身份。tests/audit-b01-b05.test.ts 保存业务回归。scripts 的共享证据模块供验证与台账生成复用。

## 实施步骤
先运行未修改业务代码的反例，再修共享规则和相关入口；补日期边界、订单修订、核销与历史歧义记录测试；最后执行完整验证并打包。

## 验证命令及预期
定向测试先出现五个业务失败、两个正向通过；修复后全绿。npm run validate:release 执行类型、编译、全量测试、安全扫描、确定性 AI-contract 和规格 fixture 门禁。npm run capabilities 只接受当前源码与工具链匹配的成功记录。

## 实际验证证据
修复前等价测试：5 失败、2 通过（baseline.log、baseline.json、baseline-tests.ts.txt）。完整门禁记录于 artifacts/audit-b01-b05/validation.json：111 项测试通过，0 失败/跳过/取消；typecheck、build、security:scan、test:ai、fixtures:verify 均退出 0。能力生成已通过当前源码和工具链复核。前两轮 15 个测试文件字节未变，见 prior-tests-preserved.json。

## Surprises & Discoveries
用户附件另指出新增文件未进入旧 source_files 时仍可生成有效台账；纳入本轮 A01。外部活动标识是现有导入合同，解析订单时保留这一兼容性。第一次全量门禁发现旧仓储调用可以省略可选活动日期；修复为明确缺省 null，未改旧测试。首轮门禁记录保留在 first-gate/。证据脚本测试的子进程须清除继承的 NODE_TEST_CONTEXT，使独立测试进程输出可统计报告；该测试隔离修正也保留原失败日志。

## Decision Log
订单业务身份包含租户、门店、来源和外部编号，订单修订仍属同一业务订单。第一方触点执行含边界的七天窗口；已核验券属于订单证据。日期允许明确的 null 表示无边界，非法非空输入不得被默认值替换。

## 回滚与幂等
保留上一版源码包及历史证据。新增归因字段兼容旧数据；旧记录不猜测歧义来源。所有新测试使用隔离合成数据，不产生真实渠道写入。

## Outcomes & Next Checkpoint
本轮 B01–B05 与 A01 修复和全量离线验证完成。下一阶段选择明确的客户交付流程，补生产初始化、必要数据层、实际业务 worker 与界面，再做干净安装、浏览器及获准联调。生产数据库、业务 worker、真实模型与渠道、生产初始化和客户完整界面仍未完成。
