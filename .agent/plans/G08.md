# G08 ExecPlan — scoped control reports

## 目标和用户可见结果
七个有类型处理器、白名单问数、引用钻取、日报版本和最多三项可跟踪任务。

## 仓库现状与依赖
首轮 router 仅返回通用指标文本，历史报告/任务未保存 store scope；必须修复，不能称为七个技能完成。

## Progress
- [x] router/报告持久化初稿。
- [x] 分技能处理、租户/门店 scope、输入上限、来源/截止完整性。
- [x] 追加式指标快照、报告钻取、最多三项跟进任务和幂等。
- [x] 离线回归与证据；真实指标口径/新鲜度和日报 UAT 仍待确认。

## 设计与文件路径
packages/domain/src/control*.ts、packages/db/src/repository.ts、apps/web/src/server.ts。

## 实施步骤
在 G07 后完善指标服务、处理器与报告事务，增加跨门店、缺数据、边界限制回归。

## 验证命令及预期
typecheck、test:g07-g10 和全套指标金标；每条数字可钻取，不接受越权查询。

## 实际验证证据
npm run typecheck、npm run test:g07-g10 与完整 npm test 通过；报告版本、缺数据和注入边界已覆盖。证据见 artifacts/G08/commands.txt、daily-report-sample.json。

## Surprises & Discoveries
初稿用泛化 orders 字符串冒充来源、把空订单算成零、忽略请求 store_id，均需修复。

## Decision Log
确定性离线处理器为 DEMO 能力；无配置的真实模型保持 unavailable。

## 回滚与幂等
报告只追加版本；相同数据重建不重复创建待办。

## Outcomes & Next Checkpoint
G08 离线切片已完成并可回归；因缺少甲方确认的指标口径、源新鲜度协议和真实日报 UAT 数据集，仍保持 in_progress。
